# Spec Validation Fixtures

This directory is reserved for curated P3D fixtures used by validator tests.

Planned fixture slots:

- `mlod_good_<object_type>.p3d`
- `mlod_missing_<lod_id>.p3d`
- `mlod_empty_required_lod.p3d`

No real samples are stored yet.
